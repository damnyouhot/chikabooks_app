# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Android build
**/build/
**/.gradle/
android_broken/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/app/build

# ----------------------------------------------------------
# [중요] 아래 내용은 보안과 협업을 위해 반드시 추가해야 합니다.
# ----------------------------------------------------------

# 1. 내 컴퓨터의 SDK 경로 정보 (다른 컴퓨터와 충돌 방지)
local.properties
android/local.properties

# 2. Firebase 설정 파일 (보안 정보 포함 - 깃허브 업로드 금지)
google-services.json
android/app/google-services.json

# 3. 앱 서명 키 (유출되면 해킹 위험)
*.jks
key.properties
*.keystore

# 4. Firebase 서비스 계정 키 (절대 커밋 금지)
**/serviceAccountKey.json
**/service-account*.json